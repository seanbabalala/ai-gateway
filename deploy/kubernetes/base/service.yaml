apiVersion: v1
kind: Service
metadata:
  name: siftgate
  labels:
    app.kubernetes.io/name: siftgate
    app.kubernetes.io/part-of: siftgate
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 2099
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: siftgate
